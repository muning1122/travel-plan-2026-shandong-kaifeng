# 路线页面协作规则

1. 新增或重构任何路线详情页前，必须先阅读根目录 `ROUTE_DESIGN_STANDARD.md`。
2. 首页路线使用独立详情页；首页卡片整张可点击，不把所有详情堆进 `index.html`。
3. 存在多个方案时，方案卡必须可点击切换，并同步更新总控、地图、每日行程、住宿、交通、门票、景点、预算和应急内容。
4. 每条新路线必须通过 `ROUTE_DESIGN_STANDARD.md` 的发布前验收清单，不能发布半成品或不闭环方案。
5. 站点修改集中提交、集中验证、单次部署，避免重复触发 GitHub Actions 邮件。
